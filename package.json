{
  "name": "micro-app-handle-window-open",
  "scripts": {
    "start-base": "cd base-app && npm run serve",
    "start-child": "cd child-app && npm run serve",
    "start": "npm run start-child & npm run start-base"
  }
}
